<tr id="<%= item.id %>">
	<td><%= item.days_left %></td>
	<td><%= item.name %></td>
	<td><%= link_to "", [item.user, item], method: :delete, remote: true, class: 'glyphicon glyphicon-ok' %> </td>
</tr>