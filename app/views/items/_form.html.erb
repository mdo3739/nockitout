<%= form_for [new_item.user, new_item], remote: true, html: {class: "form-inline"} do |f| %>
	<div class='form-group'>
		<%= f.label :task %>
		<%= f.text_field :name, placeholder: 'More things to do?' , class: "form-control"%>
	</div>
	<!-- <div class='form-group'> -->
		<%= f.submit "Create Mission", class: 'btn btn-success' %>
	<!-- </div>-->
<% end %>